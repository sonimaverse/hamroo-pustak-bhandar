$kind: http-request
description: Retrieve a paginated list of approved wholesale users. Admin only.
url: "{{baseUrl}}/api/v1/wholesale/approved?page=1&limit=10"
method: GET
headers:
  Authorization: Bearer {{adminAccessToken}}
queryParams:
  page: "1"
  limit: "10"
order: 2000
